[flake8]
exclude =
    # No need to traverse our git directory
    .git,
    # There's no value in checking cache directories
    __pycache__,
    # This contains builds of srv6-sdn-controller-state that we don't want to check
    build,
    # This contains builds of srv6-sdn-controller-state that we don't want to check
    dist